Hi <%= @user.username %>

Please visit the following URL to access your account with username "<%= @user.username %>":

<%= @url %>

The account "<%= @user.username %>" was first created on <%= @user.created_at.try(:strftime, '%-d %b %Y') %>.

Rainfall records (<%= @user.locations.not_deleted.count %>):
<% @user.locations.not_deleted.order('updated_at DESC').each do |location| %>
- <%= "[#{location.id}] #{location.title} - #{location.town_suburb.try(:titleize)}, #{location.country.try(:titleize)} - #{location.created_at.try(:strftime, '%b %Y')}" %>
<% end %>

This is an automatically generated email.

Regards
Rainfall Record
---
